scrapper:
  scheduler:
    enable: true
    interval: 10s
    force-check-delay: 10s

server:
  port: 9091

spring:
  application:
    name: Scrapper

springdoc:
  swagger-ui:
    path: /swagger-ui

logging:
  config: classpath:log4j2-plain.xml
